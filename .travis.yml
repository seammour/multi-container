sudo: required
services:
  - docker

before_install:
  - docker build -t seammour/multi-container-test -f ./client/Dockerfile.dev ./client

script:
  - docker run seammour/multi-container-test npm run test -- --coverage

after_success:
    - docker built -t seammour/multi-container-client ./client
    - docker built -t seammour/multi-container-nginx ./nginx
    - docker built -t seammour/multi-container-server ./server
    - docker built -t seammour/multi-container-worker ./worker
    # log in to docker cli
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # take those images and push them to docker hub
    - docker push seammour/multi-container-client
    - docker push seammour/multi-container-nginx
    - docker push seammour/multi-container-server
    - docker push seammour/multi-container-worker
    
